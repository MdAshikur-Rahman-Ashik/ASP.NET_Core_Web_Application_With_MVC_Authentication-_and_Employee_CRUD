@model List<CoreWebApp.Models.ViewModels.UserWithRolesViewModel>

<h1 class="mb-4">Users with Roles</h1>

<table class="table table-bordered table-hover">
    <thead class="table-primary">
        <tr>
            <th scope="col">User ID</th>
            <th scope="col">User Name</th>
            <th scope="col">Roles</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.UserId</td>
                <td>@user.UserName</td>
                <td>
                    @foreach (var role in user.Roles)
                    {
                        <span class="badge bg-secondary me-1">@role</span>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>


@* @model List<CoreWebApp.Models.ViewModels.UserWithRolesViewModel>

<h1>Users with Roles</h1>

<table class="table">
    <thead>
        <tr>
            <th>User ID</th>
            <th>User Name</th>
            <th>Roles</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.UserId</td>
                <td>@user.UserName</td>
                <td>
                    @foreach (var role in user.Roles)
                    {
                        <span>@role</span>

                        <br />
                    }
                </td>
            </tr>
        }
    </tbody>
</table>


 *@